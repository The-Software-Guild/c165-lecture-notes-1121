// notes.js


// react components re-renders automatically when 3 things happen
// 1) if that component's state changes/updated
// 2) if that component's props are changed or in general passed down
// 3) If ANY of its parents' or higher up anchestors in a branch re-render


// listening enedlsy to events without cleaning them === memory leak

// e.prevtDefault()


this.state = {
     seconds: null
}

this.state.seconds = //update logic

setState()


// SPA


// component lifecycle 
// 3 stages =>

// they dont exist on the page => they do exist on the page => they dont exist on the page 




// ->        // mounting



// ->         // unmounting 



























const testRandom = (itteration, cb) => {
     // conditional logic that accounts for edge cases if NOT already in random7
     // testing conditions 
     // loop that runs random7 a itteration # of times
     // outputs to the console ONLY when a number is outside of 1 and 7 exclusively 
     // or some type of distrubtion list/susccess message 
}


testRandom(100, random7) // 









